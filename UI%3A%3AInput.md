# ui
> rectangle-shaped.  
> MUST NOT overlapped each other

# toggle
> where mouse pointer got inside of `ui` from outside of it.

# click
> where mouse pointer has event of `ClickDown` and `ClickUp` ALL inside a certain ui.