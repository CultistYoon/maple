# 데이터 편집

<!-- 출처: reference/TaskWiki.md · 문서 #141 -->


학습 과정 소개
데이터 셋을 이용해 데이터를 추가하거나 수정할 수 있습니다. 이번 가이드에서는 데이터의 생성 및 편집 방법, 스크립트로 데이터를 불러오는 방법에 대해 알아봅니다.

데이터 셋 생성 및 데이터 에디터 열기
Workspace - MyDesk의 콘텍스트 메뉴에서 Create DataSet을 클릭합니다.

생성된 데이터를 더블클릭하여 데이터 에디터를 엽니다.
2

데이터 에디터
3


번호	설명
NO01	컬럼 셀
각 열의 데이터명, 즉 컬럼 명을 정의합니다.
각 열의 데이터를 가져올 때 컬럼 명을 통해 가져올 수 있습니다.
NO_02	셀
각 셀은 인풋 텍스트 필드로 각 열의 정의에 적합한 데이터 값을 입력합니다.
입력한 데이터 값은 모두 스트링으로 저장됩니다.
NO_03	행/열 추가 버튼
버튼을 누르면 행/열 끝에 새로운 행이나 열을 추가합니다.
NO_04	행/열 삽입 및 삭제 핸들러
선택한 행/열을 기준으로 상하/좌우에 새로운 행/열을 추가하거나 선택한 행/열을 삭제할 수 있습니다.
핸들러는 각 머리 행 또는 머리 열을 선택하면 노출됩니다.
NO_05	웹에서 데이터 가져오기
GoogleSpreadSheet의 데이터를 가져올 수 있습니다.
GoogleSpreadSheet에서 데이터를 작성한 후 해당 데이터의 URL을 팝업창에 넣어주면
현재 열려있는 데이터에 GoogleSpreadSheet의 데이터를 덮어씌웁니다.
단, GoogleSpreadSheet의 URL은 웹에 게시한 URL만 사용할 수 있습니다.
NO_05_1
NO_06	CSV 파일 불러오기
컴퓨터에 저장된 CSV 파일을 가져와 현재 데이터에 덮어씌울 수 있습니다.
utf-8로 저장된 CSV 파일만 정상적으로 가져올 수 있습니다.
CSV 파일 내보내기
메이커의 DataSet을 CSV 파일로 내보내어 컴퓨터에 저장할 수 있습니다.


작성한 데이터 스크립트에서 불러오기
먼저 위에 생성한 NewDataSet에 다음과 같이 데이터를 작성합니다.
6


데이터를 저장하고 새 스크립트 컴포넌트 DataSet을 추가합니다.
newcomponent


DataSet 컴포넌트를 열고, OnBeginPlay() 함수를 추가합니다.
8


다음과 같이 작성합니다.

[server only]
void OnBeginPlay()
{
    --위에 생성한 데이터의 이름으로 데이터 테이블을 가져옵니다.
    local dataSet = _DataService:GetTable("NewDataSet")    
    --가져오려는 셀 데이터의 행, 열 번호를 매개 변수로 입력해 줍니다.
    local cellValue = dataSet:GetCell(3,2)               
    --셀 값을 콘솔창에 출력합니다.
    log("cellValue : "..tostring(cellValue))             
}


Hierarchy - map01의 콘텍스트 메뉴에서 Add Component를 클릭합니다.


DataSet을 검색하여 DataSet 컴포넌트를 추가합니다.
8-3

더 알아보기
_DataService:GetTable(string name)
데이터의 이름으로 데이터 테이블을 리턴하는 함수입니다.
매개 변수로 데이터 이름을 넘겨줍니다.

UserDataSet:GetCell(int row_1based, int col_based)
데이터에서 특정 셀의 값을 문자열 타입으로 리턴합니다.
매개 변수로는 셀이 위치한 행, 열 번호를 입력해 줍니다.
행과 열은 1부터 시작합니다.


플레이를 눌러 콘솔 창을 확인합니다.
9DB
Data DB 저장 및 불러오기
DataStorage 활용하기
DataStorage 사용량 제한 알아보기
DataStorage 사용량 제한 활용하기
DataStorage 사용량 제한 활용하기 2
