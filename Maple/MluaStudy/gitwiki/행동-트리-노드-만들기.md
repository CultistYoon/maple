# 행동 트리 노드 만들기

> 📖 **메이플스토리 월드 공식 제작 가이드 문서입니다.** 더 자세하거나 최신 내용이 필요하면 공식 가이드/구글에서 **「행동 트리 노드 만들기」** 로 검색하세요 — 이 페이지 제목은 공식 문서 제목과 동일합니다.
> _분류: 05 게임로직시스템 · 출처: MapleStory Worlds 공식 위키_

---

학습 과정 소개
행동 트리를 만드는 데 사용하는 노드에 대해 알아보고, 직접 예제를 따라하며 행동 트리에 따라 움직이는 AI 몬스터를 만들어 봅시다.

참고 가이드
행동 트리를 활용한 AI 만들기

참고 API Reference
BTNode

AIComponent

ParallelNode

RandomSelectorNode

SelectorNode

SequenceNode

행동 트리 만들기
Composite Node 생성
Composite Node 객체는 각 노드의 생성자를 통해 생성할 수 있습니다. Composite Node의 생성자는 다음과 같습니다.

Node	Constructor
Sequence	SequenceNode(string name = "SequenceNode")
Selector	SelectorNode(string name = "SelectorNode")
RandomSelector	RandomSelectorNode(string name = "RandomSelectorNode")
Parallel	ParallelNode(string name = "ParallelNode")
[server only]
void OnBeginPlay()
{
	local seqNode = SequenceNode("sq")
	local selNode = Selector("sel")
	local randomNode = RandomSelector("rand")
	local prNode = Parallel("pr")
}
AttachChild()
Composite Node는 자식 노드를 가질 수 있습니다. 자식 노드를 만들 때는 AttachChild()를 활용합니다. Composite Node는 Action Node, Composite Node도 자식으로 가질 수 있으며 이를 이용해 복잡한 트리 구조를 형성할 수 있습니다.

AttachChildAt()
트리 구조는 자식 노드를 실행할 때 자식 노드로 등록된 순서를 따르므로 등록 순서를 유의해야 합니다. 노드의 실행 순서를 변경 방법은 두 가지입니다. 첫 번째로 코드상에서 순서를 변경합니다. 두 번째로 AttachChildAt() 함수를 활용합니다.

AttachChildAt()는 등록 순서와 관계없이 특정 위치에 노드를 등록할 수 있습니다. AttachChildAt() 함수의 기본 사용 방식은 AttachChild()와 동일합니다. 차이점은 두 번째 매개 변수로 노드가 위치할 순서를 넘겨주는 것입니다.
순서는 0부터 시작하며, 이미 등록된 노드와 같은 위치에 새 노드를 등록하면 기존 노드의 위치가 하나씩 뒤로 밀려납니다.
[object Object]

[server only]
void OnBeginPlay()
{
	--CREATE NODE
	local sequence = SequenceNode("sq")
	local actionA = self:CreateNode("ActionA")
	local actionB = self:CreateNode("ActionB")
	local actionC = self:CreateNode("ActionC")
	local actionD = self:CreateNode("ActionD")
	
	--SET TREE
	sequence:AttachChild(actionA)
	sequence:AttachChild(actionB)
	sequence:AttachChild(actionC)
	sequence:AttachChildAt(actionD,1)
}
Node 제거하기
이미 등록된 자식 노드를 제거할 때는 DetachChild(), DetachChildAt()를 활용합니다.

DetachChild()는 매개 변수로 떼어낼 자식 노드 객체를 넘겨 자식 노드를 제거합니다.

DetachChildAt()은 떼어낼 자식 노드 객체의 위치를 넘겨 자식 노드를 제거합니다.

[object Object]

[server only]
void OnBeginPlay()
{
	--CREATE NODE
	local sequence = SequenceNode("sq")
	local actionA = self:CreateNode("ActionA")
	local actionB = self:CreateNode("ActionB")
	local actionC = self:CreateNode("ActionC")
	
	--SET TREE
	sequence:AttachChild(actionA)
	sequence:AttachChild(actionB)
	sequence:AttachChild(actionC)
	
	--DETACH NODE
	sequence:DetachChildAt(1)
	sequence:DetachChild(actionC)
}
Root Node 설정
Root Node는 SetRootNode()를 사용합니다. 매개 변수로 Root 노드가 노드를 최상위 노드로 넘겨줍니다. 최상위 노드를 Root 노드로 설정함으로써 엔티티에 AI를 부여하고 행동 트리를 작동시킬 수 있습니다.

[server only]
void OnBeginPlay()
{
	--CREATE NODE
	local sequence = SequenceNode("sq")
	local actionA = self:CreateNode("ActionA")
	local actionB = self:CreateNode("ActionB")
	local actionC = self:CreateNode("ActionC")
	
	--SET TREE
	sequence:AttachChild(actionA)
	sequence:AttachChild(actionB)
	sequence:AttachChild(actionC)
	
	--SET ROOT NODE
	self:SetRootNode(sequence)
}
Decorator Node 생성
Decorator Node는 BTNodeType을 활용해 생성할 수 있습니다. 새 BTNodeType을 생성하고, 프로퍼티로 any 타입의 Child 또는 child를 추가해 Decorator Node로 만들 수 있습니다.
자식 노드를 실행하고 싶을 때는 Behave() 함수를 호출합니다.

Property: 
[None]
any Child = nil

Method: 
any OnBehave(number delta)
{
	if self.Child == nil then
		return BehaviourTreeStatus.Failure
	end
	
	local result = self.Child:Behave(delta)
	
	if result == BehaviourTreeStatus.Success then
		return BehaviourTreeStatus.Failure
	
	elseif result == BehaviourTreeStatus.Failure then
		return BehaviourTreeStatus.Running
	else
		return BehaviourTreeStatus.Success
	end
}
생성된 Decorator BTNodeType의 객체 노드 생성과 자식 노드 할당은 아래와 같이 할 수 있습니다.

[server only]
void OnBeginPlay()
{
	local aiComponent = self.Entity.AIComponent
	
	local childNode = aiComponent:CreateLeafNode("childNode", function(delta) return BehaviourTreeStatus.Success end)
	local decorator = self:CreateNode("Decorator")
	decorator.Child = childNode
}
행동 트리 완성
Root Node 설정까지 마치면, 만든 행동 트리를 기반으로 AIComponent가 동작합니다.
15

[server only]
void OnBeginPlay()
{
	--CREATE COMPOSITE NODE
	local sequence = SequenceNode("Sequence")
	local sequenceA = SequenceNode("SequenceA")
	local sequenceB = SequenceNode("SequenceB")
	local sequenceC = SequenceNode("SequenceC")
	local selectorA = SelectorNode("SelectorA")
	local randomSelectorA = RandomSelectorNode("RandomSelectorA")
	local parallelA = ParallelNode("ParallelA")
	local parallelB = ParallelNode("ParallelB")
		
	--CREATE ACTION NODE
	local actionA1 = self:CreateNode("ActionA", "ActionA1")
	local actionA2 = self:CreateNode("ActionA", "ActionA2")
	local actionB1 = self:CreateNode("ActionB", "ActionB1")
	local actionB2 = self:CreateNode("ActionB", "ActionB2")
	local actionC1 = self:CreateNode("ActionC", "ActionC1")
	local actionC2 = self:CreateNode("ActionC", "ActionC2")
	local actionD1 = self:CreateNode("ActionD", "ActionD1")
	local actionD2 = self:CreateNode("ActionD", "ActionD2")
	local actionE = self:CreateNode("ActionE", "ActionE")
	local actionF = self:CreateNode("ActionF", "ActionF")
	local actionG = self:CreateNode("ActionG", "ActionG")
	
	--SET TREE
	sequence:AttachChild(sequenceA)
	sequence:AttachChild(parallelA)
	sequenceA:AttachChild(randomSelectorA)
	sequenceA:AttachChild(actionA1)
	parallelA:AttachChild(sequenceB)
	parallelA:AttachChild(actionB1)
	parallelA:AttachChild(selectorA)
	randomSelectorA:AttachChild(sequenceC)
	randomSelectorA:AttachChild(actionA2)
	sequenceB:AttachChild(actionB2)
	sequenceB:AttachChild(actionC1)
	selectorA:AttachChild(parallelB)
	selectorA:AttachChild(actionD1)
	sequenceC:AttachChild(actionC2)
	sequenceC:AttachChild(actionD2)
	parallelB:AttachChild(actionE)
	parallelB:AttachChild(actionF)
	parallelB:AttachChild(actionG)
}
행동 트리 활용 예제
AIComponent와 BTNodeType을 활용해 일정 범위 내에서 플레이어를 감지하고, 배회하는 AI 몬스터를 만들어 봅시다. 이 예제의 주요 구현 요소는 세 가지입니다.

플레이어가 멀리 있을 때 몬스터는 무작위 방향으로 이동하며 배회합니다.

일정 거리 안으로 플레이어가 들어오면 몬스터는 플레이어를 추적합니다.

플레이어가 멀어지면 몬스터는 추적을 그만두고 다시 주위를 배회합니다.

[object Object]

사전 준비
RectTile 모드로 전환하고, 타일을 배치합니다.
[object Object]

Workspace - MyDesk - Create Material을 선택해 새로운 Alert 머티리얼을 추가합니다.

Shader: Rainbow

Blend: 0.5

Rotate: 0

Spread: 0.25

TimeOffset: 0

TimeScale: 1

Material

Preset List - Monster - monster2358 프리셋을 추가합니다. 프리셋의 컴포넌트 중 TransformComponent, StateAnimationComponent, SpriteRendererComponent만 남겨 놓은 뒤 아래 3개 컴포넌트를 추가합니다.

StateComponent, MovementComponent, KinematicbodyComponent

monster

AIPatrolComponent 만들기
Workspace - BaseEnvironment - NativeScripts - Component - AIComponent를 선택해 Extend합니다. 이름을 AIPatrolComponent로 변경합니다.

맵에 배치한 몬스터에 AIPatrolComponent를 추가합니다.
AIPatrolComponent

DrawRange() 함수를 추가합니다. PolygonRendererComponent를 활용해 몬스터가 플레이어를 감지할 수 있는 범위를 보여줄 수 있도록 아래와 같이 작성합니다.

Property:
[None]
number DetectionRange = 5

Method:
[server only]
void DrawRange()
{
    ---@type PolygonRendererComponent
    local polygon = self.Entity:AddComponent("PolygonRendererComponent")
    polygon.Color = Color(0, 1, 0, 0.25)

    local res = 24
    local radius = self.DetectionRange
    for i = 1, res do
        local rad = 2 * math.pi * (i / tonumber(res))
        local x = math.cos(rad) * radius
        local y = math.sin(rad) * radius
        polygon.Points:Add(Vector2(x, y))
    end
}
Flip() 함수를 생성합니다. 몬스터의 이동 방향에 따라 몬스터의 스프라이트를 좌우 반전할 수 있도록 아래와 같이 작성합니다.

[server only]
void Flip()
{
    local kb = self.Entity.KinematicbodyComponent
    local sprite = self.Entity.SpriteRendererComponent

    if kb.MoveVelocity:Magnitude() > 0 then
        if kb.MoveVelocity.x < 0 then
            sprite.FlipX = false
        else
            sprite.FlipX = true
        end
    end
}
DetectPlayer() 함수를 생성하고, 플레이어와의 거리를 감지할 수 있도록 아래와 같이 작성합니다.
플레이어와의 거리가 Range 이하면 플레이어 엔티티를 메모리에 기록하고, 거리가 Range 이상이면 플레이어 엔티티를 메모리에서 제거합니다. 감지한 플레이어는 ActionFollow, DecoHasTarget, DecoHasNoTarget에서 사용합니다.

[server only]
void DetectPlayer()
{
    local target = nil
    local minDist = self.DetectionRange
    local myPos = self.Entity.TransformComponent.WorldPosition:ToVector2()

    local users = _UserService.UserEntities
    for userId, user in pairs(users) do
        local userPos = user.TransformComponent.WorldPosition:ToVector2()
        local dist = userPos:Distance(myPos)
        if dist < minDist then
            target = user
            minDist = dist
            break
        end
    end

    if target == nil then
        self:SetMemory("PlayerInRange", nil)
    else
        self:SetMemory("PlayerInRange", target)
    end
}
OnUpdate를 추가하고, DetectPlayer(), Flip() 함수를 호출하도록 아래와 같이 작성합니다.

void OnUpdate(number delta)
{
    self:DetectPlayer()
    self:Flip()
}
OnBeginPlay를 추가하고, DrawRange() 함수를 호출하도록 아래와 같이 작성합니다.

void OnBeginPlay()
{
    self:DrawRange()
}
Memory 프로퍼티를 추가합니다.

Property:
[None]
table Memory = {}
SetMemory() 함수를 생성하고 value를 key에 저장하도록 아래와 같이 작성합니다.

[server only]
void SetMemory(string key, any value)
{
    self.Memory[key] = value
}
GetMemory()를 생성하고, key에 저장된 value를 얻도록 아래와 같이 작성합니다.

[server only]
any GetMemory(string key)
{
    return self.Memory[key]
}
BTNodeType 만들기
BTNodeType을 활용해 아래의 트리 구조를 만들어 봅시다. 예제에서 만들 BTNodeType은 총 8개입니다. 이 노드들이 서로 동작하며 맵에서 움직이고, 플레이어를 추적하게 됩니다.

Workspace - MyDesk - Create Scripts - BTNodeType을 선택해 새로운 8개의 BTNodeType을 만듭니다. 8개의 BTNodeType은 아래와 같이 각기 다른 역할을 합니다.

노드	설명
ActionChangeStateAlert	몬스터의 머티리얼을 변경합니다. 감지 범위는 빨간색으로 변경됩니다.
ActionChangeStateCalm	몬스터의 머티리얼을 원래대로 되돌립니다. 감지 범위를 나타내는 원의 색상을 초록색으로 변경합니다.
ActionFollow	플레이어를 추적합니다.
ActionMoveRandom	무작위 방향으로 이동합니다.
ActionStop	정지합니다.
ActionWait	일정 시간 동안 대기합니다.
DecoHasNoTarget	감지된 플레이어가 없으면 자식 노드를 실행하고, 있으면 Failure를 반환하는 Decorator입니다.
DecoHasTarget	감지된 플레이어가 있으면 자식 노드를 실행하고, 없으면 Failure를 반환하는 Decorator입니다.
ActionChangeStateAlert
ActionChangeStateAlert에 몬스터의 외형이 Rainbow 머티리얼로 변경되고, 감지 범위인 PolygonRendererComponent의 색상이 변경될 수 있도록 아래와 같이 작성합니다.

Method:
any OnBehave(number delta)
{
    local sprite = self.ParentAI.Entity.SpriteRendererComponent
    local polygon = self.ParentAI.Entity.PolygonRendererComponent

    if sprite == nil or polygon == nil then
        return BehaviourTreeStatus.Failure
    end

    sprite:ChangeMaterial("material://EntryID")
    polygon.Color = Color(1, 0, 0, 0.25)

    return BehaviourTreeStatus.Success
}
ActionChangeStateCalm
몬스터의 감지 범위에 플레이어 엔티티가 없을 때 감지 범위는 초록색, 몬스터의 머티리얼을 꺼진 상태가 될 수 있도록 아래와 같이 작성합니다.

Method:
any OnBehave(number delta)
{
    -- 몬스터의 외관을 변경합니다.
    -- 스프라이트의 머티리얼을 기본 머티리얼로, 폴리곤 렌더러의 색상을 초록색으로 변경합니다.

    local sprite = self.ParentAI.Entity.SpriteRendererComponent
    local polygon = self.ParentAI.Entity.PolygonRendererComponent

    if sprite == nil or polygon == nil then
        return BehaviourTreeStatus.Failure
    end

    sprite:ChangeMaterial(nil)
    polygon.Color = Color(0, 1, 0, 0.25)

    return BehaviourTreeStatus.Success
}
ActionFollow
범위 안에 플레이어가 들어왔을 때 플레이어를 추적할 수 있도록 아래와 같이 작성합니다. 감지된 플레이어를 확인하기 위해 AIPatrolComponent:GetMemory()를 사용합니다.

Method:
any OnBehave(number delta)
{
    ---@type AIPatrolComponent
    local parentAI = self.ParentAI
    local transform = parentAI.Entity.TransformComponent
    local target = parentAI:GetMemory("PlayerInRange")

    if target == nil then
        return BehaviourTreeStatus.Failure
    end

    local diff = target.TransformComponent.WorldPosition - transform.WorldPosition
    diff = diff:ToVector2()
    local dist = diff:Magnitude()

    if dist > 0 then
        local dir = diff:Normalize()
        local movement = self.ParentAI.Entity.MovementComponent
        movement:MoveToDirection(dir, 0)
        return BehaviourTreeStatus.Running
    end

    return BehaviourTreeStatus.Success
}
ActionMoveRandom
몬스터가 다양한 방향으로 이동할 수 있도록 아래와 같이 작성합니다.

Property:
[None]
Vector2 MoveDirection = Vector2(0,0)
[None]
number MoveTime = 0
[None]
number ElapseTime = 0

Method:
void OnInit()
{
    -- 실행할 때마다 이동 방향과 시간을 새롭게 결정합니다.

    local randomX = _UtilLogic:RandomDouble() - 0.5
    local randomY = _UtilLogic:RandomDouble() - 0.5
    
    self.MoveDirection = Vector2(randomX, randomY):Normalize()
    self.MoveTime = _UtilLogic:RandomDouble() * 2 + 2
    self.ElapsedTime = 0
    
    local movement = self.ParentAI.Entity.MovementComponent
    movement:MoveToDirection(self.MoveDirection, 0)
}

any OnBehave(number delta)
{
    -- OnInit()에서 결정된 방향과 시간만큼 이동합니다.
    -- 플레이어와 닿으면 success를 반환합니다.
    
    local movement = self.ParentAI.Entity.MovementComponent
    
    self.ElapsedTime += delta
    
    if self.ElapsedTime >= self.MoveTime then
        movement:MoveToDirection(Vector2.zero, 0)
        return BehaviourTreeStatus.Success        
    end

    return BehaviourTreeStatus.Running       
}
ActionStop
몬스터가 움직임을 정지할 수 있도록 아래와 같이 작성합니다.

Method:
any OnBehave(number delta)
{
    -- 정지합니다.
    local movement = self.ParentAI.Entity.MovementComponent

    if movement ~= nil then
        movement:Stop()
    end

    return BehaviourTreeStatus.Success
}
ActionWait
일정 시간 동안 동작을 멈추고 대기합니다. 일정 시간이 경과하기 전까지 Running을 반환하고, 시간이 경과하면 Success를 반환하도록 아래와 같이 작성합니다.

Property:
[None]
number ElapsedTime = 0
[None]
number Time = 0

Method:
void OnInt()
{
    self.ElapsedTime = 0
}

any OnBehave(number delta)
{
    self.ElapsedTime += delta

    if self.ElapsedTime >= self.Time then
        return BehaviourTreeStatus.Success
    else
        return BehaviourTreeStatus.Running
    end
}
DecoHasNoTarget
감지된 플레이어가 없으면 자식 노드를 실행하고, 있으면 Failure를 반환할 수 있도록 아래와 같이 작성합니다.

Property:
[None]
any Child = nil

Method:
any OnBehave(number delta)
{
    ---@type AIPatrolComponent
    local parentAI = self.ParentAI
    local target = parentAI:GetMemory("PlayerInRange")

    if target == nil then
        return self.Child:Behave(delta)
    else
        return BehaviourTreeStatus.Failure
    end
}
DecoHasTarget
감지된 플레이어가 있으면 자식 노드를 실행하고, 없으면 Failure를 반환할 수 있도록 아래와 같이 작성합니다.

Property:
[None]
any Child = nil

Method:
any OnBehave(number delta)
{
    ---@type AIPatrolComponent
    local parentAI = self.ParentAI
    local target = parentAI:GetMemory("PlayerInRange")

    if target == nil then
        return BehaviourTreeStatus.Failure
    else
        return self.Child:Behave(delta)
    end
}
행동 트리 만들기
AIPatrolComponent에 새로운 CreateBehaviourTree() 함수를 추가합니다. 트리 구조를 생성할 수 있도록 아래와 같이 작성합니다.

[server only]
void CreateBehaviourTree()
{
    local root = SelectorNode("Root")

    local seqAttack = SequenceNode("Attack")
    local actAlertState = self:CreateNode("ActionChangeStateAlert", "ChangeState: Alert")
    local actFollow = self:CreateNode("ActionFollow", "Follow")

    seqAttack:AttachChild(actAlertState)
    seqAttack:AttachChild(actFollow)

    local decoHasTarget = self:CreateNode("DecoHasTarget", "HasTarget (Attack)")
    decoHasTarget.Child = seqAttack

    local seqPatrol = SequenceNode("Patrol")
    local seqMove = SequenceNode("Move")
    local actCalmState = self:CreateNode("ActionChangeStateCalm", "ChangeState: Calm")
    local actStop = self:CreateNode("ActionStop", "Stop")
    local actMoveRandom = self:CreateNode("ActionMoveRandom", "MoveRandom")
    local actWait = self:CreateNode("ActionWait", "Wait")
    actWait.Time = 3

    seqMove:AttachChild(actWait)
    seqMove:AttachChild(actMoveRandom)

    local decoHasNoTarget = self:CreateNode("DecoHasNoTarget", "HasNoTarget (Move)")
    decoHasNoTarget.Child = seqMove

    seqPatrol:AttachChild(actCalmState)
    seqPatrol:AttachChild(actStop)
    seqPatrol:AttachChild(decoHasNoTarget)

    root:AttachChild(decoHasTarget)
    root:AttachChild(seqPatrol)

    self:SetRootNode(root)
}
OnBeginPlay()함수에서 CreateBehaviourTree() 함수를 호출하도록 아래와 같이 수정합니다.

void OnBeginPlay()
{
    self:CreateBehaviourTree()
    self:DrawRange()
}StateSet 제어하기
StateSet은 엔티티의 상태를 제어할 때 사용합니다. StateSet Editor를 활용하면 보다 간편하게 상태 머신의 흐름을 시각적으로 구성할 수 있습니다.

참고 가이드
타입의 이해

엔티티의 상태 제어하기

참고 API Reference
StateComponent

StateChangeEvent

StateSet
StateSet은 상태를 추가하고, 상태 사이의 전이 조건을 정의할 수 있습니다. 크리에이터는 3가지 프리셋과 기본 StateSet들을 활용할 수 있습니다. 크리에이터가 정의한 StateSet을 StateComponent의 StateSetId 프로퍼티 값으로 설정하면, 플레이 시점에 로드됩니다.

[object Object]

이름	설명
StateSet	크리에이터가 정의할 수 있는 기본 StateSet입니다.
Player Preset	플레이어 엔티티의 기본 상태 머신이 구현된 프리셋입니다. 기본 플레이어 동작을 확장할 때 사용할 수 있습니다.
ChaseAI Preset	플레이어를 추적하는 AIChaseComponent를 사용한 몬스터 프리셋의 상태 머신입니다.
WanderAI Preset	주변을 배회하는 AIWanderComponent를 사용한 몬스터 프리셋 모델 상태 머신입니다.
StateSet Editor
StateSet Editor에서 상태 노드를 추가하고, 전이를 선으로 연결해 상태 머신을 제작할 수 있습니다. StateSet Editor를 여는 방법은 2가지입니다.

Panels - StateSet Editor

Workspace - MyDesk - 생성한 StateSet 더블 클릭

Workspace - MyDesk - 생성한 StateSet - Property 창 - Open StateSetEditor 버튼 클릭

StateSetEditor

StateSet Editor의 주요 영역은 다음과 같습니다.

번호	이름	설명
[object Object]	StateSet 추가	새로운 StateSet을 생성할 수 있습니다.
[object Object]	StateSet 선택	편집할 StateSet을 선택할 수 있습니다. StateSet을 편집 중에 다른 StateSet으로 변경할 경우, 변경 사항을 저장한 뒤 선택해야 합니다.
[object Object]	+ Node 버튼	좌측 상단의 버튼으로 새로운 상태 노드를 추가합니다.
[object Object]	Scale 슬라이더	캔버스 영역의 확대/축소 배율을 조절합니다.
[object Object]	캔버스	상태 노드를 배치하고, 노드들의 전이를 편집할 수 있는 편집 화면입니다.
[object Object]	편집 창	노드, 전이의 프로퍼티 창입니다.
상태 노드
상태 노드는 IsNative 값에 따라 NativeStateType 또는 ScriptStateType으로 지정할 수 있습니다.

상태 노드 생성
상태 노드는 두 가지 방법으로 생성할 수 있습니다. 추가한 노드의 상태를 설정창에서 변경할 수 있습니다.

캔버스에서 마우스 우클릭 - Create State Node 선택

좌측 상단의 [+ Node] 버튼 클릭

시작 노드 지정
State가 어떤 노드부터 시작할지를 결정하는 시작 노드를 반드시 지정해야 합니다. 시작 노드로 정할 노드를 선택하고, 컨텍스트 메뉴에서 Set Start Node를 선택합니다. 시작 노드는 색상이 파란색으로 변경됩니다.
[object Object]

상태 노드 이름
모든 상태 노드는 StateSet 내에서 중복되지 않는 고유한 이름을 가져야 합니다. 이름은 대문자 알파벳, 숫자, _ 조합만 가능합니다.

주의
다음 키워드는 네이티브 예약어로 사용되고 있어 주의가 필요합니다. 네이티브 예약어를 사용하면 Name 필드 좌측에 경고 아이콘이 표시됩니다.
"IDLE", "ATTACK", "ATTACK_WAIT", "CROUCH", "DEAD", "FALL", "CLIMB", "LADDER", "HIT", "JUMP", "MOVE", "SIT"

상태 노드 종류 구분
NativeStateType
IsNative를 true로 설정하면 NativeStateType에 지정된 값들 중 하나를 선택할 수 있습니다. NativeStateType은 크리에이터가 동작을 수정할 수 없으므로 Player Preset처럼 플레이어의 상태를 확장하는 경우 외에 사용하지 않는 것을 권장합니다. NativeStateType의 값들은 아래와 같습니다.

타입	설명
Idle	정해진 행동이 없는 상태
Move	이동 상태
Jump	점프 상태
Fall	낙하 상태
Climb	로프 등반 상태
Ladder	사다리 등반 상태
Crouch	엎드린 상태
Sit	앉아 있는 상태
Attack	공격 중인 상태
AttackWait	공격 후 딜레이 상태
Hit	피격 상태
Dead	사망 상태
DoNothing	기본값
ScriptStateType
IsNative를 false로 설정하면 ScriptStateTypeId에서 크리에이터가 생성한 StateType을 선택할 수 있습니다. StateType 생성과 활용 방법은 엔티티의 상태 제어하기를 참고합니다.
ScriptStateType은 StateComponent.IsLegacy 값에 따라 이벤트 실행 가능 여부가 결정됩니다. StateComponent.IsLegacy가 true인 경우 서버 플레이어 엔티티, 서버 스폰 엔티티, 클라이언트 스폰 엔티티의 이벤트를 OnUpdate()에서 호출할 수 있습니다.

[object Object]

전이
전이는 특정 상태에서 조건을 만족했을 때 다음 상태로 이동할 수 있게 상태와 상태를 연결합니다.

전이 생성
StateSet Editor에서 노드와 노드를 연결해 전이를 생성할 수 있습니다. 조건이 설정되면 전이를 나타내는 선이 흰색으로 표시되고, 조건이 없는 경우 노란색으로 표시됩니다.

전이를 생성할 State를 선택 - 컨텍스트 메뉴 - Make Transition을 선택합니다.
[object Object]

생성된 선을 연결할 도착 노드로 드래그하고, 도착 노드를 선택합니다.
[object Object]

연결된 선을 선택하고, 프로퍼티 창에서 전이의 프로퍼티 값과 조건을 설정합니다.

프로퍼티
Previous Node Name: 전이가 시작되는 노드입니다. 전이를 생성하면 값이 자동으로 결정됩니다.

Next Node Name: 전이 도착 노드입니다. 생성 시 자동으로 결정됩니다.

OrderInTransit: 전이의 평가 순서를 결정합니다. 값이 높은 전이부터 먼저 평가됩니다. 같은 값을 가진 전이끼리는 순서가 보장되지 않으며, 전이 내부의 조건들은 작성된 순서대로 평가됩니다.

TransitIfAnyConditionIsTrue: 여러 조건을 어떻게 결합할지 결정합니다. 값이 true인 경우 조건 중 하나라도 참으로 평가되면 전이를 수행합니다. false인 경우 모든 조건이 참으로 평가되어야 전이를 수행합니다.

Conditions: 전이가 발생할 수 있는 조건을 추가합니다. NativeConditionType, ScriptConditionType이 있습니다.

NativeConditionType
Condition의 IsNative를 true로 설정하면 NativeConditionType으로 전이를 사용하게 됩니다. NativeCondition 전이는 조건이 이미 정의되어 있으므로 크리에이터가 동작을 수정할 수 없습니다.
[object Object]

타입	설명
Always	항상 참으로 평가되는 조건
HasOnetimeAnimDone	애니메이션이 한 차례 재생을 마치면 참으로 평가
HasTimePassed	Duration에 설정된 시간이 경과되면 참으로 평가
IsOnGround	땅 위에 있을 경우 참으로 평가
IsStopped	정지 상태일 경우 참으로 평가
IsNotClimbing	등반 중이 아닐 경우 참으로 평가
Never	절대 참으로 평가되지 않는 조건
ScriptConditionType
Condition의 IsNative를 false로 설정하면, ScriptConditionTypeId에서 ConditionType를 선택할 수 있습니다. ConditionType 전이를 사용하면 크리에이터가 전이 발생 조건을 직접 정의할 수 있습니다. 또한 ParentComponent 프로퍼티를 통해 StateComponent에 접근할 수 있습니다.

[object Object]

StateComponent와의 관계
크리에이터가 생성한 StateSet은 StateComponent.StateSetId에 지정되어야 사용이 가능합니다. 또한, StateComponent의 IsLegacy 값에 따라 StateSet에 정의된 StateType과 ConditionType의 이벤트 실행 가능 공간이 달라집니다.

StateType과 StateComponent
각 실행 공간에서 StateType의 OnEnter(), OnUpdate(), OnExit() 이벤트의 실행 여부는 상태 머신을 실행하는 엔티티의 속성과 StateComponent.IsLegacy 프로퍼티에 의해 결정됩니다.
StateType은 ParentComponent 프로퍼티를 통해 상태 머신을 구동 중인 StateComponent 인스턴스에 접근하여 로직에 필요한 다양한 정보를 참조할 수 있습니다.

StateComponent.IsLegacy가 true일 때

구분	OnEnter 호출	OnUpdate 호출	OnExit 호출
PlayerControllerComponent가 있는 엔티티(서버)	⭕	❌	⭕
PlayerControllerComponent가 있는 엔티티(플레이어의 클라이언트)	⭕	⭕	⭕
PlayerControllerComponent가 있는 엔티티(타플레이어의 클라이언트)	⭕	⭕	⭕
PlayerControllerComponent가 없는 엔티티(서버)	⭕	⭕	⭕
PlayerControllerComponent가 없는 엔티티(클라이언트)	⭕	❌	⭕
로컬화된 엔티티	⭕	❌	⭕
StateComponent.IsLegacy가 false일 때

구분	OnEnter 호출	OnUpdate 호출	OnExit 호출
PlayerControllerComponent가 있는 엔티티(서버)	⭕	⭕	⭕
PlayerControllerComponent가 있는 엔티티(플레이어의 클라이언트)	⭕	⭕	⭕
PlayerControllerComponent가 있는 엔티티(타플레이어의 클라이언트)	⭕	⭕	⭕
PlayerControllerComponent가 없는 엔티티(서버)	⭕	⭕	⭕
PlayerControllerComponent가 없는 엔티티(클라이언트)	⭕	⭕	⭕
로컬화된 엔티티	⭕	⭕	⭕
ConditionType과 StateComponent
OnConditionCheck()는 StateComponent 내부의 상태 머신이 현재 상태에서 다른 상태로의 전이를 시도할 때 호출되는 실제 조건 검사용 이벤트입니다. 상태 머신을 실행하는 엔티티의 속성과 StateComponent.IsLegacy 프로퍼티에 따라 어느 하나의 실행 공간에서만 호출됩니다.
ConditionType은 ParentComponent 프로퍼티를 통해 상태 머신을 구동 중인 StateComponent 인스턴스에 접근하여 로직에 필요한 다양한 정보를 참조할 수 있습니다.

StateComponent.IsLegacy가 true일 때

구분	OnConditionCheck 호출
PlayerControllerComponent가 있는 엔티티(서버)	❌
PlayerControllerComponent가 있는 엔티티(플레이어의 클라이언트)	⭕
PlayerControllerComponent가 있는 엔티티(타플레이어의 클라이언트)	❌
PlayerControllerComponent가 없는 엔티티(서버)	⭕
PlayerControllerComponent가 없는 엔티티(클라이언트)	❌
로컬화된 엔티티	❌
StateComponent.IsLegacy가 false일 때

구분	OnConditionCheck 호출
PlayerControllerComponent가 있는 엔티티(서버)	❌
PlayerControllerComponent가 있는 엔티티(플레이어의 클라이언트)	⭕
PlayerControllerComponent가 있는 엔티티(타플레이어의 클라이언트)	❌
PlayerControllerComponent가 없는 엔티티(서버)	⭕
PlayerControllerComponent가 없는 엔티티(클라이언트)	❌
로컬화된 엔티티	⭕
활용 예제
일정 확률로 두 가지 아이템 중 하나가 나타나는 상태 머신을 만들어봅시다. Panels - Package Storage - MSW 패키지 - Example-StateSet을 설치하면 완성된 결과를 확인할 수 있습니다.
[object Object]

Gacha StateSet 만들기
2가지 확률이 나오기 위해서는 준비, 실행, 결과 1, 결과 2 총 4가지 노드가 필요합니다.

새로운 Gacha StateSet을 생성하고, 시작 노드의 이름을 READY로 변경합니다.

새로운 노드 3개를 생성하고 이름을 각각 TRY, SPAWN_SCROLL, SPAWN_POTION으로 설정합니다.
Node

Workspace - MyDesk - Create Scripts - Create Logic을 눌러 NetworkLogic을 생성하고 아래와 같이 작성합니다.

Method
boolean HasAuthority(Entity entity)
{
		local hasAuthority = false

		if entity ~= nil then
			local entityOwnerId = entity.OwnerId
			if _UtilLogic:IsNilorEmptyString(entityOwnerId) then
				if entity.Localize then
					hasAuthority = self:IsClient()
				else
					hasAuthority = self:IsServer()
				end
			else
				local localPlayerEntity = _UserService.LocalPlayer
				if localPlayerEntity ~= nil then
					hasAuthority = (entityOwnerId == localPlayerEntity.OwnerId)
				end
			end
		end

		return hasAuthority

}
Workspace - MyDesk - Create Scripts - Create StateType을 눌러 새로운 SpawnModel을 생성합니다.

Property:
string ModelId = ""
string Name = ""
Vector3 SpawnPosition = Vector3(0,0,0)
Entity Parent = nil
Entity Spawned = nil

Method:
void OnEnter()
{
    local parent = self.Parent
    if parent == nil then
        parent = self:GetParentEntity()
        if parent == nil then
            return
        end
    end

    if parent.Localize then
        if _NetworkLogic:IsServer() then
            return
        end
    else
        if _NetworkLogic:IsClient() then
            return
        end
    end

    self.Spawned = _SpawnService:SpawnByModelId(self.ModelId, self.Name, self.SpawnPosition, parent)
}

Entity GetParentEntity()
{
    local parentEntity = nil

    local parentStateComponent = self.ParentComponent
    if parentStateComponent ~= nil then
        parentEntity = parentStateComponent.Entity
    end

    return parentEntity
}

TransformComponent GetParentTransformComponent()
{
    local parentTransformComponent = nil

    local parentEntity = self:GetParentEntity()
    if parentEntity ~= nil then
        parentTransformComponent = parentEntity.TransformComponent
    end

    return parentTransformComponent
}
Gacha 모델 생성
생성한 Gacha StateSet을 사용하기 위해서 StateComponent가 있는 엔티티를 생성합니다.

Workspace - MyDesk - Create Model을 눌러 새로운 Gacha 모델을 생성합니다.

모델에 TransformComponent, RigidbodyComponent, SpriteRendererComponent, StateComponent를 추가합니다.

SpriteRUID: 939597de7475400ab64231404c764e4c

StateComponent의 StateSetId에 Gacha를 지정합니다.

맵에 모델을 배치합니다.

전이 생성
전이 조건으로 활용할 ConditionType을 만들어 봅시다. 특정 시간이 될 때마다 상태를 변경해줄 HasTimePassed, 아이템이 나타나는 확률을 결정해 줄 BasicRandom 두 가지가 필요합니다.

Workspace - MyDesk - Create Scripts - Create ConditionType을 눌러 HasTimePassed를 생성합니다.

생성한 HasTimePassed에 아래와 같이 작성합니다.

Property:
number Duration = 1
number OnPreviousStateEnteredTime = 0

Method:
void OnPreviousStateEnter()
{
    self.OnPreviousStateEnteredTime = _UtilLogic.ServerElapsedSeconds
}

boolean OnConditionCheck(string nextStateName)
{
    return (_UtilLogic.ServerElapsedSeconds - self.OnPreviousStateEnteredTime) >= self.Duration
}
Workspace - MyDesk - Create Scripts - Create ConditionType을 눌러 BasicRandom을 생성합니다.

생성한 BasicRandom에 아래와 같이 작성합니다.

Property:
number Chance = 0.5
number Result = 0.5

Method:
void OnPreviousStateEnter()
{
    self.Result = _UtilLogic:RandomDouble()
}

boolean OnConditionCheck(string nextStateName)
{
    return self.Result <= self.Chance
}
Reward StateSet 만들기
새로운 Reward StateSet을 생성하고, 시작 노드의 이름을 POPOUT으로 변경합니다.

Workspace - MyDesk - Create Scripts - Create StateType을 눌러 새로운 AddForce를 생성합니다.

아이템이 나타나는 방향과 속도를 설정할 수 있도록 아래와 같이 작성합니다.

Property:
Vector2 Force = Vector2.zero

Method:
void OnEnter()
{
    local parentRigidbodyComponent = self:GetParentRigidbodyComponent()
    if parentRigidbodyComponent == nil then
        return
    end

    if _NetworkLogic:HasAuthority(parentRigidbodyComponent.Entity) == false then
        return
    end

    parentRigidbodyComponent:AddForce(self.Force)
}

Entity GetParentEntity()
{
		local parentEntity = nil

		local parentStateComponent = self.ParentComponent
		if parentStateComponent ~= nil then
		    parentEntity = parentStateComponent.Entity
		end

		return parentEntity
}

RigidbodyComponent GetParentRigidbodyComponent()
{
    local parentRigidbodyComponent = nil

    local parentEntity = self:GetParentEntity()
    if parentEntity ~= nil then
        parentRigidbodyComponent = parentEntity.RigidbodyComponent
    end

    return parentRigidbodyComponent
}
Potion, Scroll 모델 생성
Gacha 모델의 상태가 SPAWN_POTION, SPAWN_SCROLL로 변경되었을 때 맵에 나타날 아이템들을 생성해 봅시다.

Workspace - MyDesk - Create Model을 눌러 새로운 Potion, Scroll 모델을 생성합니다.

모델 에디터에서 TransformComponent, RigidbodyComponent, SpriteRendererComponent, StateComponent를 추가합니다.

Potion SpriteRUID: c1f920047b71463e87232c4f42cf8241

Scroll SpriteRUID: 071abdd0474e4933ab44d53eb7d349f9

StateComponent의 StateSetId에 Reward를 지정합니다.

Gacha 상태 머신 완성
Gacha StateSet의 노드들에 아래와 같이 값을 설정합니다.

이름	IsNative	ScriptStateTypeId	프로퍼티
READY	true	-	NativeStateType: DoNothing
TRY	true	-	NativeStateType: DoNothing
SPAWN_POTION	false	SpawnModel	ModelId: Potion 모델의 Entry ID
Name:Potion
SpawnPosition -0.3, 0,0
Parent: nil
SPAWN_SCROLL	false	SpawnModel	ModelId: Scroll 모델의 Entry ID
Name:Scroll
SpawnPosition -0.3, 0,0
Parent: nil
각 노드를 선택 - 컨텍스트 메뉴 Make Transition을 선택하고, 아래 그림과 같이 연결합니다.
[object Object]

READY → TRY: 3초의 대기 상태 이후에 TRY로 상태를 변환합니다.

TRY → READY: TRY 상태에서 READY로 변환합니다.

TRY → SPAWN_SCROLL: 50%의 확률로 TRY에서 SPAWN_SCROLL로 전환합니다.

TRY → SPAWN_POTION: 50%의 확률로 TRY에서 SPAWN_POTION으로 전환합니다.

SPAWN_POTION → READY: 아이템이 나타나면 READY 상태로 돌아갑니다.

SPAWN_SCROLL → READY: 아이템이 나타나면 READY 상태로 돌아갑니다.

연결한 전이들에 아래와 같이 값을 입력합니다.
[object Object]

방향	OrderInTransit	Condition
READY → TRY	0	IsNative: false
ScriptConditionType: HasTimePassed
Duration:3
TRY → READY	0	IsNative: true
NativeConditionType: Always
TRY → SPAWN_POTION	1	IsNative: false
ScriptConditionType: BasicRandom
Chance:0.5
TRY → SPAWN_SCROLL	2	IsNative: false
ScriptConditionType: BasicRandom
Chance:0.5
SPAWN_POTION → READY	0	IsNative: true
NativeConditionType: Always
SPAWN_SCROLL → READY	0	IsNative: true
NativeConditionType: Always
Reward 상태 머신 완성
Reward StateSet의 ScriptStateTypeId에 AddForce를 지정하고, Force 값을 입력합니다.
popout

이름	IsNative	ScriptStateTypeId	프로퍼티
POPOUT	false	AddForce	Force: -2.5, 3.5
Gacha 모델을 맵에 배치하고, 3초마다 두 가지 모델 중 하나가 나타나는지 확인합니다.
