# 이동 발판 만들기

> 📖 **메이플스토리 월드 공식 제작 가이드 문서입니다.** 더 자세하거나 최신 내용이 필요하면 공식 가이드/구글에서 **「이동 발판 만들기」** 로 검색하세요 — 이 페이지 제목은 공식 문서 제목과 동일합니다.
> _분류: 02 지형과이동 · 출처: MapleStory Worlds 공식 위키_

---

학습 과정 소개
움직이는 발판을 만들어 이동에 재미를 더해보세요.
CustomFootholdComponent의 IsDynamicFoothold 프로퍼티를 이용해 나만의 움직이는 발판을 만들 수 있습니다.
발판 만들기에서 기본 발판 제작 방법을 배울 수 있습니다.

이동 발판 만들기
이동 발판은 CustomFootholdComponent를 활용해 크리에이터가 직접 만들어야 합니다.
발판의 움직임을 만드는 방식은 다르지만, 기본 CustomFootholdComponent를 움직이게 만들기 위해선 IsDynamicFoothold, RigidbodyMovementOption 프로퍼티를 설정해야 합니다.

프로퍼티 명	설명
IsDynamicFoothold	움직이는 발판을 만들기 위해 활성화합니다.
RigidbodyMovementOption	움직이는 발판 위의 플레이어 엔티티가 어떤 관성을 받을 것인지 방식을 적용합니다.
RelativeX: x축 관성은 적용하고, y축 관성은 적용하지 않습니다. 플레이어가 발판의 이동 방향과 반대로 움직이면 속도가 감속하고, 같은 방향으로 움직이면 가속합니다.
Absolute: 플레이어가 어느 방향으로 움직여도 속도가 동일합니다.
RelativeX	Absolute
reletiveX	absolute
활용 예시
발판으로 사용할 오브젝트에 CustomFootholdComponent를 추가하고, 발판을 만듭니다.

CumstomFootholdComponent - IsDynamicFoothold를 활성화합니다.

CumstomFootholdComponent - RigidbodyMovementOption을 RelativeX로 설정합니다.
01

새로운 컴포넌트를 생성하고 발판 오브젝트에 추가합니다.

새로운 컴포넌트의 스크립트 에디터 창을 열고, 발판의 이동 시간과 범위를 아래와 같이 작성합니다.
TransformComponent:Translate(0,0)의 x 값만 입력할 경우 좌우로 이동하고, y 값만 입력할 경우 상하로 움직입니다. 두 값을 모두 입력하면 대각선으로 움직입니다.

Property:
[Sync]
number totalTime = 0
    
Method:
[Server only]
void OnUpdate(number delta)
{
    self.totalTime = self.totalTime + delta
    if self.totalTime > 0 and self.totalTime <= 5.0 then
        self.Entity.TransformComponent:Translate(0,0.009)
    elseif self.totalTime > 5.0 and self.totalTime <= 10.0 then
        self.Entity.TransformComponent:Translate(0,-0.009)
    elseif self.totalTime > 10 then
        self.totalTime = 0
    end
}
