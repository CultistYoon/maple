# 물리 사용하기

> 📖 **메이플스토리 월드 공식 제작 가이드 문서입니다.** 더 자세하거나 최신 내용이 필요하면 공식 가이드/구글에서 **「물리 사용하기」** 로 검색하세요 — 이 페이지 제목은 공식 문서 제목과 동일합니다.
> _분류: 05 게임로직시스템 · 출처: MapleStory Worlds 공식 위키_

---

학습 과정 소개
메이플스토리 월드의 맵과 엔티티에 물리 법칙 적용 방법을 알아봅시다. 물리 법칙을 활용해 엔티티가 서로 충돌해 튕기거나, 포물선 운동을 하며 날아가거나, 엔티티가 서로 연결되어 움직이게 하는 등 물리를 활용한 움직임을 다양하게 만들어낼 수 있습니다.
엔티티의 충돌과 충돌 그룹 만들기를 함께 학습하길 권장합니다.

맵에 물리 법칙 적용하기
기본 맵은 메이플스토리 맵 구성과 이동 원칙을 따르므로 물리 법칙은 기본 적용되어 있지 않습니다. 그러므로 물리 법칙을 적용하고 싶은 맵에 별도의 설정이 필요합니다. 물리 기능을 맵에서 사용하고 싶다면 반드시 해당 맵에 component PhysicsSimulatorComponent를 추가해야 합니다.
component PhysicsSimulatorComponent의 Gravity 프로퍼티를 활용해 맵의 중력을 설정할 수 있습니다. Gravity의 x, y 값에 따라 맵의 중력 방향과 힘이 달라집니다. 만약 물리 법칙이 적용되는 모든 엔티티가 중력을 왼쪽 방향으로 작용하는 맵을 만들고 싶다면 x 값을 음수로 지정하고, 중력이 위로 작용하는 맵을 만들고 싶다면 y 값을 양수로 지정합니다. x, y 값을 0으로 설정해 무중력 맵을 만들 수도 있습니다.

hierachy Hierachy - world World - maps maps - map map01을 선택합니다.

PhysicsSimulatorComponent를 추가하고, Gravity 값을 입력합니다.
simulator

(-5,-10)	(0,4)
-5	+4
지면 만들기
기본 월드의 설정은 물리 엔티티들과 상호 작용이 일어나지 않기 때문에 기본 발판도 마찬가지로 물리 엔티티가 충돌하지 않고 뚫고 지나가게 됩니다. 물리 엔티티가 타일에 충돌하게 하려면 map TileMapComponent, component CustomeFootholdComponent의 PhysicsInteractable를 활성화 합니다.
map TileMapComponent의 PhysicsInteractable을 활성화 해 물리 엔티티들이 충돌할 수 있는 지면과 벽 같은 역할을 할 수 있게 만들어봅시다.

hierachy Hierachy - world World - maps maps - map map01 - map TileMap을 선택합니다.

mapcomponent TileMapComponent의 PhysicsInteractable 프로퍼티를 checkbox활성화합니다.
interactable

PhysicsInteractable checkboxdisable	PhysicsInteractable checkbox
off	on
엔티티에 물리 법칙 적용하기
엔티티에 PhysicsRigidbodyComponent를 엔티티에 추가해 엔티티의 물리 타입, 마찰력 등을 조정할 수 있습니다. 이때 PhysicsColliderComponent가 함께 추가되며 충돌체의 모양과 크기를 설정할 수 있습니다. 두 컴포넌트를 추가한 엔티티는 충돌할 수 있고, 관련 Event를 발생시킬 수 있습니다. 만약 PhysicsRigidbodyComponent를 추가하지 않는다면 질량이 무한히 큰 Static 타입 엔티티가 됩니다. 물리 엔티티는 세가지 타입으로 나뉘며 아래와 같은 특성이 있습니다.

Static: 질량이 무한히 큰 엔티티로 취급되며 Dynamic 타입과 충돌할 수 있습니다.
static

Dynamic: 엔티티가 물리 법칙에 따라 동작합니다. 물리 충돌이 가능한 다른 Entity와 충돌할 수 있습니다.
dynamic

Kinematic: 질량이 무한히 큰 엔티티로 취급되나 Static 타입과 다르게 속도를 지정해 움직일 수 있습니다
kinematic

참고 가이드
엔티티에 물리 적용하기

다양한 물리 Joint 활용하기

엔티티의 충돌
